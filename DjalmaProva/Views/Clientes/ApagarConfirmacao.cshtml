@model Cliente
@{
    ViewData["Title"] = "Confirmação apagar o Cliente ?";
}

<div class="text-center">
    <h3>Realmente deseja apagar o contato (@Model.NomeCompleto)? </h3>
</div>

<a class="btn btn-primary" asp-controller="Clientes" asp-action="Index">Cancelar exclusão</a>
<a class="btn btn-danger" asp-controller="Clientes" asp-action="Apagar" asp-route-id="@Model.ClienteId">Confirma exclusão</a>
